# CJ40076 软件功能说明

## 1. 文档说明

本文档说明 `CJ40076GCC` 工程当前代码实际实现的功能、操作方式、数据处理规则和维护注意事项。

- 工程目录：`D:\gt\CJ40076\version1\CJ40076GCC`
- 固件名称：CJ40076
- 硬件版本宏：CJ40076-V2.0
- 软件版本宏：0.1.0
- MCU：N32L403KBQ7，Cortex-M4F
- 操作系统：FreeRTOS
- 编译系统：CMake + GNU Arm Embedded Toolchain
- 文档核对日期：2026-08-13

> 注意：本文描述的是当前工作区源码，包括尚未提交的连续测距、首末目标和临时测试模式修改，不等同于测距机手册列出的全部能力。固件版本宏目前仍是 `0.1.0`，代码功能发生变化时需要人工同步更新版本号。

## 2. 系统组成

系统由主控、测距机、IMU/电子罗盘、GNSS、段码 LCD、按键、电池采样和电源控制组成。

| 模块 | 当前用途 | 接口/速率 |
| --- | --- | --- |
| N32L403KBQ7 | 主控、任务调度、协议解析、坐标计算和显示控制 | Cortex-M4F，硬浮点 |
| DYC-15A 测距机 | 自检、单次测距、连续测距、首/末目标输出 | UART4，115200 bit/s |
| JY901B | 横滚角、俯仰角、航向角和校准 | USART2，9600 bit/s |
| BV-220 GNSS | 本机经纬度、海拔、卫星数和定位状态 | USART1，115200 bit/s |
| CS1622 段码 LCD | 距离、姿态、坐标、海拔、次数、电池和模式图标 | GPIO 模拟串行接口 |
| 电源键、模式键 | 测量启停、模式切换、关机和校准操作 | GPIO，低电平有效 |
| 电池 ADC | 电压检测、电量分档和欠压关机 | PA0 / ADC_CH_1 |
| 主机 UART | 仅有 UART5 和缓冲区配置，当前无业务功能 | UART5，115200 bit/s，未初始化/未使用 |

### 2.1 主要引脚

| 功能 | 引脚 | 有效电平 |
| --- | --- | --- |
| 电池 ADC | PA0 | 模拟输入 |
| 测距机电源 | PA7 | 高有效 |
| IMU 电源 | PA8 | 高有效 |
| GNSS 电源 | PA12 | 高有效 |
| 电源保持 | PA10 | 高有效 |
| 模式键 | PA6 | 低有效 |
| 电源键 | PA9 | 低有效 |
| GNSS TX/RX | PA4/PA5 | USART1 |
| IMU TX/RX | PA2/PA3 | USART2 |
| 测距机 TX/RX | PB0/PB1 | UART4 |
| 主机 TX/RX | PB4/PB5 | UART5 |
| LCD CS/RD/WR/DATA | PA15/PB3/PB6/PB7 | GPIO |

## 3. 软件架构

### 3.1 启动流程

上电后的主要顺序如下：

1. 清零 FreeRTOS 专用堆区，初始化 SEGGER RTT 和 CmBacktrace。
2. 先初始化电源键 PA9 并持续读取按键状态；只有检测到电源键按下才拉高 PA10 电源保持并继续初始化，其余上电/复位来源停留在开机检测阶段。
3. 初始化其余 GPIO、电池 ADC 等板级资源，创建 `startup` 启动任务并进入 FreeRTOS 调度。
4. 启动任务关闭三个外部模块，读取 Flash 中的测量计数和三个角度补偿值，并等待 3 秒电源稳定。
5. 读取一次电池电压；有效电压低于 2600 mV 时直接关机。
6. 依次执行测距机、IMU、GNSS 自检，任一失败立即关闭模块并释放电源保持。
7. 自检全部通过后创建 I/O、控制和显示任务，然后删除启动任务。

启动自检期间显示任务尚未创建，LCD 不显示自检进度或故障码。自检失败的外部表现是设备断电，具体失败模块需通过 SEGGER RTT 日志判断。

### 3.2 FreeRTOS 任务

`configMINIMAL_STACK_SIZE` 为 160 个 32 位字，FreeRTOS 动态堆为 12 KB。

| 任务 | 优先级 | 栈 | 周期/行为 | 作用 |
| --- | ---: | ---: | --- | --- |
| `startup` | 3 | 480 words，约 1920 B | 仅启动阶段 | 电池检查、自检和运行任务创建 |
| `io` | 2 | 320 words，约 1280 B | 约 5 ms | 接收并解析测距、IMU、GNSS 数据，更新时间 |
| `control` | 2 | 480 words，约 1920 B | 约 20 ms | 按键、模式、测量流程、电池和关机控制 |
| `display` | 1 | 320 words，约 1280 B | 正常约 1 s；补偿设置约 100 ms | 生成并刷新 LCD 内容、计算目标坐标 |

I/O 与控制任务同优先级并启用时间片。应用状态 `AppStateSnapshot` 使用 FreeRTOS 临界区进行整块读写，显示任务每次取得一致快照。

### 3.3 串口接收

三个模块串口采用 DMA 环形接收，并在 UART 空闲中断或任务主动轮询时搬入软件环形缓冲区。

| 串口对象 | DMA 缓冲区 | 软件缓冲区 | 中断优先级 |
| --- | ---: | ---: | ---: |
| GNSS | 512 B | 512 B | 5 |
| IMU | 128 B | 128 B | 6 |
| 测距机 | 64 B | 64 B | 7 |
| 主机 | 20 B | 20 B | 8，当前未使用 |

软件环形缓冲区满时，新字节会被静默丢弃，没有溢出计数或用户提示。因此扩展报文量、降低任务调度频率或增加日志时，需要重新评估缓冲区容量。

## 4. 启动自检

### 4.1 测距机自检

- 测距机上电后等待 500 ms。
- 最多等待 2 s，每隔 500 ms 重发一次 `0x01` 自检命令。
- 收到自检返回后，代码要求返回状态中两个指定状态位均为 1，才认为通过。
- 通过后依次查询最小门限 `0xA3` 和最大门限 `0xA5`，每条最多等待 300 ms。
- 门限查询仅用于协议预热；当前代码不保存、不显示，也不根据查询值调整测距。
- 门限查询是否成功不会影响整个自检结果。

### 4.2 IMU 自检和初始化

IMU 上电等待 500 ms 后，固件向 JY901B 写入并保存以下配置：

- 输出速率：5 Hz。
- 输出内容：仅角度帧。
- 安装方向：垂直安装（写入 `ORIENT=0x01`）。厂家协议要求垂直安装时模块 Y 轴箭头朝上，当前实物已调整为 Y 轴朝上。
- 姿态算法：明确设置为九轴算法，磁力计参与绝对航向解算。

随后清空串口接收，在 2 s 内收到一个校验正确的 `0x55 0x53` 角度帧即通过。自检只验证通信和有效角度帧，不评估角度准确性、磁场环境或校准质量。

### 4.3 GNSS 自检

GNSS 上电等待 1 s 后，在 5 s 内收到任意一个串口字节即通过。

> 该条件只证明串口线上有数据，不代表收到了完整 NMEA 语句，也不代表已经获得定位。正常运行中仍必须收到校验正确、定位质量大于 0 的 `$GNGGA` 才会显示有效坐标。

## 5. 按键操作

### 5.1 公共规则

- 两个按键的消抖时间均为 40 ms。
- 电源键按下达到 3 s 立即产生长按事件，不必等待松开。
- 用于开机的首次电源键按压不会作为测量短按或关机长按；必须先松开电源键，之后再次按键才参与运行期按键判断。
- 模式键以松开作为一次点击，最后一次点击后等待 600 ms 才执行对应操作。
- 2 次以及其他未定义次数没有动作。
- 点击计数最大为 8；超过 8 次仍按 8 次处理。

### 5.2 操作表

| 按键操作 | 功能 | 注意事项 |
| --- | --- | --- |
| 模式键单击 | 单次 → 连续 → 多功能 → 测试 → 单次 | 测试模式运行时忽略模式键；关闭测试模式编译开关后只循环前三种 |
| 模式键三击 | 测量计数清零 | 只清零 RAM 中的计数，长按电源正常关机时才保存到 Flash |
| 模式键四击 | 进入角度补偿设置 | 停止测距、关闭 GNSS，保持 IMU 供电以便实时预览 |
| 模式键五击 | 加速度计校准 | 期间 LCD 全显；模块必须正面朝上、水平且保持静止约 4 秒 |
| 模式键六击 | 参考角校准 | 期间 LCD 全显，校准结果写入 IMU 内部存储 |
| 模式键七击 | 开始磁场校准 | LCD 持续全显，按 JY901B 要求转动设备，最后必须八击结束 |
| 模式键八击 | 结束磁场校准并保存 | 必须先七击成功进入磁场校准；否则八击无操作 |
| 电源键短按 | 按当前模式启动、停止或重发测量 | 各模式行为不同，见第 6 节 |
| 电源键长按 3 s | 立即关机 | 角度补偿设置界面除外；其他界面从稳定按下开始计时，满 3 s 当时执行，不等待松手 |

四击进入的角度补偿设置与五至八击的 JY901B 内部校准是两类功能。二者都会关闭测距机和 GNSS，只给 IMU 供电。除正确的“七击开始、八击结束”流程外，不建议在磁场校准尚未结束时执行其他模式键多击操作。

## 6. 工作模式

设备默认进入单次测距模式。

| 模式 | 模式图标 | 待机模块供电 | 电源键短按 | 自动结束 |
| --- | --- | --- | --- | --- |
| 单次测距 | 单次 | 测距、IMU、GNSS均关闭 | 发起一次多目标测距 | 收完一轮或 3 s 超时 |
| 连续测距 | 连续 | 测距、IMU、GNSS均关闭 | 第一次启动；运行中再次短按停止 | 发布 10 轮或总计 12 s 超时 |
| 多功能 | 单次 | IMU、GNSS常供电，测距机关闭 | 发起一次多目标测距 | 收完一轮或 5 s 超时 |
| 测试 | 单次 + 连续 | IMU、GNSS常供电，测距机关闭 | 第一次启动周期测试；再次短按停止 | 不自动停止 |

### 6.1 单次测距模式

电源键短按后执行：

1. 清除上一笔测距显示。
2. 关闭 IMU 和 GNSS。
3. 测距机上电并等待 500 ms。
4. 设置测距机为“多目标”模式，最多等待 300 ms ACK，但 ACK 超时不会中止流程。
5. 发送单次测距命令。
6. 最后一帧后静默达到 40 ms，即认为本轮数据接收完成并发布结果。
7. 若 3 s 内没有完成，则发布超时/无目标结果并关闭测距机。

单次模式中测量尚未结束时再次短按电源键，不是停止操作，而是清除当前聚合状态并重新发送一次测量流程。频繁重按可能使前后两次返回帧混在一起，应等待本次完成后再按。

### 6.2 连续测距模式

电源键第一次短按后执行：

1. 测距机上电并等待 500 ms。
2. 设置连续测量频率为 1 Hz。
3. 设置为“多目标”模式。
4. 发送连续测距启动命令。
5. 每聚合并发布一轮结果，测量计数增加 1。
6. 发布满 10 轮后发送停止命令并关闭测距机，LCD 保留第 10 轮结果。
7. 从启动连续命令开始达到 12 s 时强制结束，防止设备一直等待。

运行期间再次短按电源键会提前停止。当前代码不是“连续模式只测首目标”，而是 1 Hz 多目标测量，并只在设备端保留、显示首目标和末目标。

若 12 s 内一轮结果都没有发布，固件仍会发布一次无目标/超时结果，因此计数增加 1。若只收到少于 10 轮，则按实际已发布轮数计数，不额外补足到 10。

### 6.3 多功能模式

- 进入模式时 IMU 和 GNSS 上电并持续工作。
- 电源键短按启动一次多目标测距，测距机仅在测量期间供电。
- 单次接收静默结束条件仍为 40 ms，但总超时放宽到 5 s。
- 测距完成后，根据首/末距离、当前姿态和当前 GNSS 定位计算目标坐标与高度。
- 测距机关闭后，IMU 和 GNSS 保持供电，继续显示本机或已缓存的目标信息。

多功能模式中测量尚未结束时再次短按电源键同样不是停止，而是重新发起测量。

### 6.4 临时连续工作时间测试模式

该模式用于“每分钟测量 5 次”的连续工作时间测试，默认开启。

- 电源键第一次短按启动测试，并立即进行第一笔多功能单次测量。
- 后续测量计划间隔为 12 s，即每分钟 5 次。
- 每笔测量都使用多目标方式，目标坐标、IMU、GNSS 和 LCD 逻辑与多功能模式相同。
- 每次测量结束后关闭测距机，IMU 与 GNSS 持续供电。
- 测量占用超过计划时刻时，不并发、不补发；当前测量结束后立即开始下一次，并从该时刻重新计 12 s。
- 测试不会在固定次数后自动停止；再次短按电源键、长按关机或欠压关机才会停止。
- 测试运行期间模式键全部忽略，避免误切模式或误触发校准。
- 如果以后开启自动关机，测试运行期间会抑制自动关机。

临时模式开关位于 `inc/bsp/board_config.h`：

```c
#define APP_WORK_TIME_TEST_MODE_ENABLE 1U
```

改为 `0U` 并重新编译即可从用户模式循环中移除测试模式；相关代码通过条件编译排除。枚举值仍保留在内部类型中，不影响前三种模式运行。

### 6.5 测量中切换模式

除正在运行的测试模式外，模式键单击在 600 ms 点击窗口结束后执行切换。切换时会：

- 停止正在运行的连续测距，并关闭测距机。
- 取消单次或多功能模式中尚未完成的测量。
- 清除上一模式的测距结果和目标坐标显示缓存。
- 按新模式重新控制 IMU 和 GNSS 供电。
- IMU/GNSS 断电、重新上电时均清空协议解析器和有效状态，断电后的串口尾包不会重新写成有效姿态或定位。

因此正常模式切换不会继续显示上一模式的距离、姿态、定位或目标坐标。不过切换动作要等单击后的 600 ms 窗口结束才发生，不是按键松开瞬间发生。

## 7. 测距机协议和目标规则

### 7.1 已使用命令

| 命令 | 功能 |
| --- | --- |
| `0x01` | 自检 |
| `0x02` | 单次测距 |
| `0x03` | 设置首/末/多目标；当前正常测量均设置为多目标 `0x03` |
| `0x04` | 启动连续测距 |
| `0x05` | 停止连续测距 |
| `0xA1` | 设置连续频率；当前设置 1 Hz |
| `0xA3` | 查询最小门限，仅启动预热 |
| `0xA5` | 查询最大门限，仅启动预热 |

帧头为 `EE 16`，设备码为 `0x03`，校验为有效载荷逐字节相加后的低 8 位。

### 7.2 首目标、末目标和单目标

测距状态字节的低 4 位表示目标类型，高 4 位表示多目标编号：

| 低 4 位 | 当前处理 |
| --- | --- |
| `0x00` 单目标 | 按首目标处理，点亮 F，不再使用普通 `target` 类型 |
| `0x01` 首目标 | 保存为首目标 |
| `0x02` 末目标 | 保存为末目标 |
| `0x03` 多目标 | 编号 0 保存为首目标；当前轮中编号最高者保存为末目标；中间目标不显示 |

距离字段 `0xFFFF` 表示无效距离。协议距离由“整数米 + 0.1 米位”组成，软件转为毫米保存，所以内部最小协议分辨率为 100 mm。

对于 `0x03` 的编号 0 数据，解析器会同时允许它作为当前末目标候选；若后续收到更高编号，末目标会被更新。因此只有编号 0 时，首、末距离可能相同并在 F/E 间交替显示。

### 7.3 一轮数据的聚合边界

多目标通常由连续多帧构成，当前固件用以下时序判断一轮结束：

- 单次、多功能和测试模式：最后一帧后 40 ms 没有新测距帧，即发布本轮。
- 连续模式：目标编号从非 0 回到 0 时，先发布上一轮；或者最后一帧后 200 ms 无新帧时发布。

这两个静默时间是对测距机回包时序的假设。若某型号、某距离或干扰环境下同一轮各目标帧间隔超过阈值，可能把一轮拆成两轮，导致首末目标配对错误或计数偏大。更换测距机固件后必须用串口抓包复核。

### 7.4 结果覆盖规则

- 同一轮内先聚合首、末目标，再一次性发布给显示。
- 下一轮发布后整体覆盖上一轮，系统不保存 10 轮历史。
- 连续测量结束后只保留最后发布的一轮结果。
- 切换模式或开始新的测量时清空旧测距结果。
- 无目标或超时也会发布一条“有效的测量过程结果”，但首、末有效标志均为假，LCD 距离显示横杠。

## 8. IMU 和电子罗盘

### 8.1 数据解析

- 只解析 11 字节、帧头为 `0x55 0x53` 的角度帧。
- 对前 10 字节求和并与末字节比较，校验正确才更新状态。
- 横滚、俯仰和航向内部单位均为 0.01°。
- 最终俯仰角 = `-JY901B 原始俯仰角` + 俯仰安装补偿。符号翻转用于匹配整机定义：激光朝上为正，朝下为负。
- 最终航向角 = `-JY901B 原始航向角` + 航向安装补偿 + 航向误差补偿，随后归一化到 `(-180°, 180°]`。原始航向取反是根据水平状态下四个已知方向的实测结果确定的，用于统一 JY901B 与整机指南针的旋转正方向。
- LCD 显示时再转换为 0～359°，小数部分直接截掉。
- 目标坐标计算中的航向和俯仰都除以 `100.0`，均保留 0.01° 分辨率。

原来写死在 JY901B 驱动中的航向 `+90.00°` 已删除，安装补偿现在由可设置的 `HIt` 提供。上电优先读取当前版本 Flash 中校验有效的用户设置；Flash 设置无效、校验失败、首次使用或从旧航向公式升级时采用 `PIt=0.0°`、`HIt=+90.0°`、`HEr=0.0°`。最终航向叠加当前生效的 `HIt` 与 `HEr`。

俯仰补偿会同时影响正常俯仰显示、水平距离、目标经纬度和目标高度。两个航向补偿值会相加，并同时影响正常航向显示、方向图标和目标坐标计算。磁偏角和局部磁干扰不会由代码自动修正，需要根据实际标定结果写入“航向误差补偿”。

### 8.2 角度补偿设置

模式键四击进入设置，页面按以下顺序循环：

```text
PIt -> HIt -> HEr -> SAVE -> PIt
```

| 页面 | 参数 | 范围 | 默认值 |
| --- | --- | ---: | ---: |
| `PIt` | 俯仰安装补偿 | -90.0°～+90.0° | 0.0° |
| `HIt` | 航向安装补偿 | -180.0°～+180.0° | +90.0° |
| `HEr` | 航向误差补偿 | -180.0°～+180.0° | 0.0° |
| `SAVE` | 保存确认 | - | - |

三个参数各自独立取值并一起保存，统一以 `0.1°` 为步进：

- 电源键短按增加 `0.1°`，模式键短按减小 `0.1°`。
- 单独按住电源键或模式键，超过约 600 ms 后连续增加或减小，每 100 ms 调整 `0.1°`。
- 两键同时按住 1 s 进入下一页；切页后必须先松开两键。
- `SAVE` 页短按电源键保存并退出，短按模式键取消并退出。
- 设置界面不会因静置自动退出；在 `SAVE` 页短按电源键保存并退出，短按模式键取消并退出。

`PIt`、`HIt` 和 `HEr` 页使用以下固定区域：

- 顶部三位航向区显示页面名 `PIt`、`HIt` 或 `HEr`。
- 设置页面不点亮正常俯仰角区域的独立 `P` 图标，页面类型只通过顶部三位名称区分。
- 四位距离区显示应用当前编辑值后的实时俯仰角或最终航向角，保留 `0.1°`，但不点亮小数点。显示数值是“角度乘以 10”，例如 `1234` 表示 `123.4°`；负俯仰用距离区最左一位横杠表示负号，例如 `-123` 表示 `-12.3°`。
- 四位高程区显示当前补偿值的绝对值，单位同样是 `0.1°`，不点亮小数点；原正常俯仰角位置的 `+/-` 符号表示补偿正负。例如 `+` 与 `900` 表示 `+90.0°`，`-` 与 `15` 表示 `-1.5°`。该屏的加号由横线和竖线两个独立段组成，显示正值时软件会同时点亮两段，负值时只点亮横线。
- `SAVE` 页在四位距离区显示 `SAVE`。

实时角度随下一帧 IMU 数据更新，JY901B 当前配置为 5 Hz，LCD 在设置状态约每 100 ms 刷新一次。设置界面会屏蔽电源键的 3 s 关机事件，使电源键可以一直按住连续增加；松开长按后的电源键不会再触发短按保存。退出设置后恢复长按 3 s 关机。

进入设置时会停止当前测距、清除测距结果、关闭 GNSS 并保持 IMU 供电。编辑期间计算使用临时值，所以不需要先保存就能预览效果；取消退出后恢复原保存值。保存只负责写入 Flash，使补偿在断电重启后继续有效。

### 8.3 方位图标

LCD 用整数航向分为八个方向区间：

| 航向 | 显示方向 |
| --- | --- |
| 337～359°、0～21° | N |
| 22～66° | NE |
| 67～111° | E |
| 112～156° | SE |
| 157～201° | S |
| 202～246° | SW |
| 247～291° | W |
| 292～336° | NW |

### 8.4 JY901B 内部校准注意事项

- 五击执行协议 `CALSW=0x01` 的加速度计校准，不是独立的陀螺仪手动零偏校准。模块必须正面朝上、水平并保持静止，否则会造成加速度和横滚/俯仰角异常。
- 五击和六击是阻塞式流程，控制任务在校准延时期间不处理电池周期检测和普通按键事件。
- 七击开始磁场校准后，IMU 保持校准状态和供电，必须完成规定动作并八击结束保存。
- 磁场校准进行中只接受模式键八击结束保存，电源键短按及其他模式键点击次数均被忽略；电源键长按会放弃本次磁场校准并直接关机。
- 五至八击的校准命令会写入 IMU 内部参数，不是四击进入的主控角度补偿设置。
- 磁场校准八击退出时，固件先发送正常模式命令，等待 100 ms，再发送保存命令；RTT 会分别提示七击已开始以及八击已发送保存命令。
- 固件会检查串口命令是否完整发送，并在失败时输出 RTT 错误；JY901B 协议没有为这些写命令提供当前代码可用的业务 ACK，因此“保存命令已发送”仍不能证明传感器内部保存成功，最终应以断电重启后的实测结果为准。
- 校准动作、放置方式和旋转方法应以 JY901B 手册为准。

## 9. GNSS 定位

### 9.1 已实现的 NMEA 数据

- 只接收校验正确的 `$GNGGA` 语句，不处理 `$GPGGA`、RMC、GSA 等其他语句。
- `fix_quality > 0` 时才把定位标记为有效。
- 纬度、经度转换为带符号的十进制度 × `10^7` 保存。
- GGA 高度转换为厘米保存。
- 解析并保存卫星数量，目前只输出到 RTT 日志，不在 LCD 数字区显示。
- N/S/W 坐标保存为负值，LCD 通过 N、S、E、W 图标显示方向。

### 9.2 有效性限制

当前代码没有 GNSS 数据超时机制。只要最后一次 GGA 的 `fix_quality` 大于 0，之后即使很久没有新语句，状态仍保持有效，直到模块上下电或收到新的无定位 GGA。因此失联、遮挡和静态老数据需要在后续版本中增加时间戳超时判断。

## 10. 目标坐标计算

目标坐标只在多功能模式和测试模式中计算。必须同时具备：

- 本轮有效的首目标或末目标距离。
- 有效 IMU 角度。
- GNSS 已定位，即 `fix_quality > 0`。

### 10.1 算法

以测得的斜距 `D`、俯仰角 `pitch`、航向角 `yaw` 计算：

```text
水平距离 = D × cos(pitch)
目标高度 = 本机GNSS高度 + D × sin(pitch)
```

再以地球半径 6371000 m，使用球面大地前向公式，根据本机经纬度、水平距离和航向求目标经纬度。经度最终归一化到 `[-180°, 180°)`。

### 10.2 采样和精度注意事项

- 测距、IMU、GNSS 没有统一硬件触发或时间戳同步。
- 目标坐标是在显示任务首次看到新测距结果时计算，最多可能晚约 1 s，使用当时状态快照中的最新姿态和 GNSS 数据，不一定是激光测距瞬间的数据。
- 坐标计算后会缓存，不会随之后的 GNSS/姿态变化重新计算，直到下一次测距结果发布。
- 如果首次处理该距离结果时 GNSS 未定位或 IMU 尚无有效帧，本次目标坐标会保持无效；之后传感器恢复也不会补算，必须重新测距。
- 航向和俯仰均以 0.01° 分辨率参与计算，但这只是数据分辨率，不代表传感器实际精度达到 0.01°。
- 未修正磁偏角、设备安装偏差、激光轴与 IMU 轴不重合、GNSS 天线与激光发射点杆臂误差。
- 使用固定球形地球，没有使用 WGS-84 椭球的高精度正算。
- GNSS GGA 高度的基准和测距目标高度需求必须确认；代码未做大地水准面或基准面转换。
- 俯仰角正负方向必须通过实机确认，否则目标高度加减方向可能与安装定义相反。

该计算适合当前设备的估算显示，不能仅凭代码中的小数位数认为已经达到对应测绘精度。

## 11. LCD 显示规则

显示任务约每 1 s 刷新一次，所以 IMU 虽以 5 Hz 输出，屏幕上的姿态变化约为 1 Hz。

### 11.1 固定区域

| 区域 | 位数 | 当前显示 |
| --- | ---: | --- |
| 方位角 | 3 位 | 0～359°，仅多功能/测试模式 |
| 距离 | 4 位 | 整数米，右对齐 |
| 坐标 | 9 位 | 3 位度 + 2 位分 + 4 位分小数 |
| 高度 | 4 位 | 整数米，右对齐 |
| 测量计数 | 5 位 | 0～99999 |
| 俯仰角 | 2 位 | 绝对值整数度，负值点亮负号 |
| 电池 | 4 格 | 电压分档，不是精确 SOC |

### 11.2 距离显示

- 内部距离为毫米，LCD 使用 `distance_mm / 1000`，只显示整数米，小数部分直接截掉。
- 同时有首、末目标时：单次/连续模式每 1 s 切换 F/E，多功能/测试模式每 2 s 切换 F/E。
- 只有首目标时固定显示 F；只有末目标时固定显示 E。
- 无当前模式有效结果、测量刚启动或本轮无目标时显示 4 个横杠。
- 4 位区域只能可靠表达 0～9999 m；更高位会被丢弃，只留下低 4 位。

### 11.3 姿态显示

- 多功能和测试模式显示方位角、方向图标和俯仰角。
- 姿态无效时数字区保持空白，但角度/P 等固定符号仍会点亮。
- 正俯仰不点亮 `+`，负俯仰点亮 `-`。

### 11.4 经纬度显示格式

坐标实际格式为“度 + 分 + 分的小数”，不是度分秒：

```text
DDD° MM.mmmm′
```

例如十进制度 `116.3912345°` 会转换为约 `116° 23.4740′`。最后 4 位是“分的小数点后 4 位”，代码采用截断而非四舍五入。

LCD 同时点亮了度、分、秒和小数点符号，但数值实现没有独立的秒字段，因此“秒”图标容易造成误解。解释或验收显示时必须以 `DDD° MM.mmmm′` 为准。

- 纬度与经度每 1 s 交替显示。
- 本机定位显示 `LOCAL` 标识。
- 测得目标后显示 `TARGET`，首末目标每 2 s 切换并显示 F/E。
- 本轮有目标距离但缺少姿态或 GNSS 定位时，进入 TARGET 状态但坐标与高度显示横杠。
- 纬度和经度符号分别使用 N/S、E/W。

### 11.5 高度显示

- 高度内部单位为厘米，LCD 除以 100 后显示整数米，小数直接截掉。
- 负高度会先取绝对值，LCD 没有显示高度负号，因此海平面以下的高度会被显示为正数。
- 4 位区域超过 9999 m 时会丢弃高位。

### 11.6 模式图标

- 单次模式：单次图标。
- 连续模式：连续图标。
- 多功能模式：单次图标，表示一次触发一次测距。
- 测试模式：单次和连续图标同时点亮，作为临时测试模式识别标志。
- 瞄准标和米单位图标在所有运行模式中点亮。
- JY901B 内部校准期间所有数字段和图标全亮；四击进入的角度补偿设置使用专用双值界面。

## 12. 电池、供电和关机

### 12.1 电池采样

- ADC 参考电压按 3300 mV 计算，外部分压比按 3/2 还原。
- 每次读取采样 8 次，去掉最大值和最小值，对剩余 6 次平均。
- 启动稳定等待结束后读取一次，正常运行中每 1 s 读取一次。

### 12.2 电量档位

| 电压 | 内部 level | percent 字段 | LCD 格数 |
| --- | ---: | ---: | ---: |
| > 3900 mV | 5 | 100% | 4 格 |
| 3800～3900 mV | 4 | 80% | 4 格 |
| 3700～3799 mV | 3 | 60% | 3 格 |
| 3600～3699 mV | 2 | 40% | 2 格 |
| < 3600 mV | 1 | 20% | 1 格 |

该 `percent` 只是档位乘以 20，不是电池模型计算出的真实剩余电量。LCD 最多只有 4 格，因此 80% 和 100% 都显示 4 格。

有效采样电压低于 2600 mV 时关机。ADC 读取失败时不会触发欠压关机；当前显示逻辑也没有专门的 ADC 故障提示。

### 12.3 关机和自动关机

开机时软件先读取低有效的 PA9 电源键，只有检测到按下才拉高 PA10 锁存供电并继续启动。该逻辑依赖硬件电源键在 PA10 尚未拉高前能够临时给 MCU 供电；软件无法在 MCU 完全断电时自行检测按键。

运行中电源键经过 40 ms 消抖后开始长按计时，持续达到 3 s 时立即执行关机，不等待按键松开。关机流程会停止连续测距、关闭测距机/IMU/GNSS，把发生变化的测量计数保存到 Flash，然后拉低 PA10 电源保持。若外部硬件没有实际切断电源，软件会进入永久延时循环，不再恢复运行。

自动关机默认关闭：

```c
#define APP_AUTO_POWER_OFF_ENABLE 0U
#define APP_AUTO_POWER_OFF_MS     300000U
```

若改为开启，则任意按键事件会刷新 5 分钟活动时间；普通测量本身不刷新活动时间。测试模式正在运行时不自动关机。自动关机触发时也会先保存发生变化的测量计数。

欠压关机和启动自检失败关机不保存计数：欠压时供电已经不可靠，不适合执行 Flash 擦除；自检失败时也不应修改正常运行计数。直接拔电、外部切断电源、复位或程序故障同样不会保存本次开机以来的计数变化。

## 13. 测量计数、角度补偿和 Flash 存储

### 13.1 计数规则

- 范围为 0～99999，到达 99999 后饱和，不再增加。
- 每发布一轮测距结果增加 1，不是每次按键增加 1。
- 连续模式正常完成 10 轮会增加 10。
- 无目标或超时发布的结果也增加 1。
- 测量增量和三击清零都只修改 RAM，不立即操作 Flash。
- 长按电源正常关机时保存；自动关机以后若启用，自动关机时也保存。
- 上电读取 Flash；新格式记录无效且又不是旧格式记录时，计数初始化为 0，并使用角度补偿默认值。

### 13.2 Flash 地址

测量计数和三个角度补偿值共用片内 Flash 最后一页：

```text
地址：0x0801F800
页大小：0x800，即 2 KB
程序可用 Flash：0x08000000～0x0801F7FF，共 126 KB
记录：count（4 B）+ magic 0x434A0079（4 B）+ 三个 int16_t 补偿值（6 B）+ reserved（2 B）+ check（4 B）
记录总大小：20 B
```

当前设置格式使用 `magic 0x434A0079`。读取到校验正确的当前记录时采用 Flash 中保存的三个补偿值。固件兼容旧的 `0x434A0076` 计数记录以及 `0x434A0077/0x434A0078` 设置记录：升级时保留测量计数，但旧设置基于航向未取反的公式，不能继续沿用，因此补偿重置为 `PIt=0.0°`、`HIt=+90.0°`、`HEr=0.0°`。Flash 记录无效或校验失败且不能识别为旧格式时，计数初始化为 0，并采用相同默认补偿。下次保存后使用 `0x434A0079` 格式，之后用户设置正常保持。新格式用异或校验字段检测记录损坏，同时检查三个补偿值是否在合法范围内。

### 13.3 Flash 保存策略和风险

当前实现使用 RAM 累计计数，只在发生变化且长按电源正常关机时擦写一次；自动关机开关以后若启用，也在自动关机时擦写一次。这样测试模式持续测量不会按每笔结果擦 Flash，擦写次数主要取决于正常开关机次数。

角度补偿只在 `SAVE` 页确认时立即写入 Flash。为了遵守“测量计数只在正常关机时保存”的规则，此时写入的是上次已经持久化的计数，不会提前保存本次开机新增或清零后的计数；RAM 中的当前计数继续保留，正常关机时再写入。

构建配置已经把应用程序 Flash 强制限制为 126 KB，正式排除了最后的 `0x0801F800～0x0801FFFF` 设置页。每次发布仍应检查链接占用，确保构建没有被其他工具链配置改回 128 KB。

> **风险 1：异常掉电会丢失本次开机计数。** 只有长按关机和将来启用后的自动关机会保存。直接拔电、复位、程序故障、欠压关机或自检失败关机都会保留上次正常保存值，本次开机以来的增量或清零不会持久化。

> **风险 2：保存过程不是掉电安全事务。** 保存时仍是先擦整页，再写单份记录，没有双备份和序号。校验字段可以发现损坏，但不能在写入中途断电后恢复旧记录；下次上电会采用计数 0 和补偿默认值。代码会检查擦写返回状态并输出 RTT 错误日志，但关机流程不会因此取消断电。

Flash 擦除/编程还可能暂停从 Flash 取指。修改测量频率、串口速率或并发业务时，应实测保存期间是否会影响实时接收。

## 14. 日志、调试和故障处理

### 14.1 日志

- 应用日志输出到 SEGGER RTT 通道 0，不输出到 UART5。
- 默认日志等级为 INFO，格式包含毫秒时间、等级、任务名和模块名。
- 每次显示任务实际刷新 LCD 后，只输出当前确实存在的数据：电子罗盘有效时打印 IMU 行；测距存在有效首目标或末目标时打印测距行；GPS 数据已解析且定位有效（`valid=true`、`fix=true`）时打印 GPS 行，未定位的 `fix=0` 数据不打印。三者都没有数据时不打印。正常界面最多约每 1 s 输出一次，角度补偿设置界面最多约每 100 ms 输出一次，JY901B 内部校准全显界面最多约每 200 ms 输出一次。
- 刷新日志中的电子罗盘单位为 `0.01°`（`r_cd/p_cd/y_cd`），测距单位为毫米（`f/l`），GPS 经纬度单位为十进制度乘 `10^7`（`lat_e7/lon_e7`），高度单位为厘米（`alt_cm`），`t` 为对应数据最后更新时间（ms）。`fix/f/l` 为有效标志。
- 保留测距通信超时、自检失败、欠压关机、Flash 保存失败、校准命令发送失败以及 RTOS/断言等故障日志。
- 不再创建周期性任务 CPU/栈统计任务；FreeRTOS 配置所需的 DWT 运行计数器接口仍保留。

### 14.2 故障处理

- 集成 CmBacktrace，可通过 RTT 输出固件信息、断言和 Cortex-M4 Fault 调用栈。
- FreeRTOS malloc 失败、任务栈溢出和应用断言均记录日志后关中断并停机。
- 运行任务创建失败会关闭外部模块并进入 `APP_POWER_FAULT`。
- 没有看门狗复位、故障码持久化或 LCD 故障页。

当前空闲钩子为空，且 tickless idle 关闭，正常代码没有主动进入 Sleep/Stop/Standby 等低功耗模式，也没有配置相应的调试器保持连接功能。

## 15. 关键配置参数

主要参数集中在 `inc/bsp/board_config.h`：

| 宏 | 当前值 | 作用 |
| --- | ---: | --- |
| `APP_KEY_DEBOUNCE_MS` | 40 ms | 按键消抖 |
| `APP_MODE_MULTI_CLICK_MS` | 600 ms | 模式键多击结束窗口 |
| `APP_POWER_LONG_PRESS_MS` | 3000 ms | 电源键长按关机 |
| `APP_WORK_TIME_TEST_MODE_ENABLE` | 1 | 临时测试模式开关 |
| `APP_WORK_TIME_TEST_INTERVAL_MS` | 12000 ms | 测试测量间隔 |
| `APP_STARTUP_SETTLE_MS` | 3000 ms | 启动电源稳定等待 |
| `APP_RANGE_POWER_ON_MS` | 500 ms | 正常测距机上电等待 |
| `APP_IMU_POWER_ON_MS` | 500 ms | IMU 上电等待 |
| `APP_GNSS_POWER_ON_MS` | 1000 ms | GNSS 上电等待 |
| `APP_RANGE_COMMAND_ACK_TIMEOUT_MS` | 300 ms | 测距设置命令 ACK 等待 |
| `APP_RANGE_SINGLE_TIMEOUT_MS` | 3000 ms | 单次模式总超时 |
| `APP_MULTI_MEASURE_TIMEOUT_MS` | 5000 ms | 多功能/测试单笔总超时 |
| `APP_RANGE_CONTINUOUS_SAMPLES` | 10 | 连续模式发布轮数 |
| `APP_RANGE_CONTINUOUS_TIMEOUT_MS` | 12000 ms | 连续模式总超时 |
| `APP_RANGE_SINGLE_GAP_TIMEOUT_MS` | 40 ms | 单次一轮静默结束时间 |
| `APP_RANGE_CONTINUOUS_GAP_TIMEOUT_MS` | 200 ms | 连续一轮静默结束时间 |
| `APP_BAT_EMPTY_MV` | 2600 mV | 欠压关机阈值 |
| `APP_AUTO_POWER_OFF_ENABLE` | 0 | 自动关机开关 |
| `APP_AUTO_POWER_OFF_MS` | 300000 ms | 自动关机等待时间 |

修改参数后要同时验证测距机协议能力、供电启动时间、按键体验和任务阻塞时间，不应只确认编译通过。

## 16. 构建和产物

常用构建命令：

```powershell
cmake --build D:\gt\CJ40076\version1\CJ40076GCC\cmake-build-debug-n32
```

构建生成：

- `CJ40076.elf`：调试和下载所需 ELF。
- `CJ40076.hex`：Intel HEX 固件。
- `CJ40076.bin`：纯二进制固件。
- `CJ40076.map`：链接映射，用于核对 Flash/RAM 和计数页冲突。

默认目标使用 Cortex-M4、Thumb、`fpv4-sp-d16` 和 hard-float。应用 Flash 限制为 126 KB，芯片最后 2 KB 留给设置记录；RAM 按 24 KB 配置，FreeRTOS 使用 `heap_4` 和 12 KB 专用堆区。

2026-08-13 的当前 Debug 构建结果为：Flash 62,080 B / 126 KB（48.12%），RAM 20,624 B / 24 KB（83.92%）。RAM 只剩约 3,952 B 的链接余量，新增全局缓冲区、任务栈或第三方库前必须重新检查 map 文件，并结合 FreeRTOS 最小剩余堆判断运行余量。

## 17. 当前未实现或未启用的能力

以下内容不要误认为已经可用：

- UART5 主机命令、数据上传或日志输出。
- 10 轮连续测距历史存储和回看。
- 中间目标显示。
- 测距门限设置、保存和界面配置；启动时仅查询门限。
- GNSS 数据老化超时、差分状态解析或 RTK 状态显示。
- 测距、姿态和 GNSS 的严格同步采样。
- 自动磁偏角模型、杆臂误差和高程基准修正；手动俯仰/航向安装补偿和航向误差补偿已经实现。
- 自动休眠或低功耗待机；自动关机宏也默认关闭。
- 看门狗和自动故障恢复。
- 自检进度、模块故障码和 ADC 故障的 LCD 提示。

## 18. 维护注意事项汇总

按风险优先级，后续开发和测试应先关注：

1. **保持应用 Flash 为 126 KB。** 当前构建已经保留最后 2 KB，后续修改工具链或工程配置时不能把应用区域改回完整 128 KB。
2. **确认计数丢失策略可接受。** 只有长按关机和将来启用后的自动关机保存，直接断电、复位和欠压关机会丢失本次开机增量。
3. **实测一轮多目标帧间隔。** 单次 40 ms、连续 200 ms 的分轮规则依赖测距机实际回包时序。
4. **不要从显示小数位推断定位精度。** 经纬度显示为分后 4 位，目标精度仍受 GNSS、测距、姿态、同步和安装误差共同限制。
5. **处理 GNSS/IMU 数据新鲜度。** 当前只判断最后状态有效，不判断数据是否超时。
6. **需要测量同步时调整架构。** 当前坐标可能使用测距完成后约 1 s 的姿态/GNSS 快照。
7. **单次和多功能模式没有短按停止。** 测量中重按会重发，不应按连续模式的启停逻辑操作。
8. **测距设置 ACK 超时被忽略。** 频率或目标模式设置失败时仍会启动测量，现场异常需结合 RTT 和串口抓包判断。
9. **LCD 有截断和溢出限制。** 距离/高度只显示整数米，超过 4 位丢高位；负高度不显示负号。
10. **区分软件补偿与 JY901B 校准。** 四击只编辑主控补偿值；磁场校准仍要七击开始、完成旋转后八击结束保存。
11. **软件版本需要人工更新。** 当前新增功能后版本宏仍为 0.1.0，交付时应与固件标签、测试记录一致。
12. **扩展串口业务时检查并发。** 应监测 DMA/软件缓冲溢出，并明确 I/O 与控制任务共享测距聚合状态的同步方式。
13. **RAM 余量有限。** 当前 Debug 构建已使用 83.92% RAM；修改任务栈、接收缓冲或数学/日志功能后要同时检查链接占用和运行时最小剩余堆。

## 19. 建议实机验证清单

### 19.1 启动与基础硬件

- 正常模块全部连接时能通过自检并进入单次模式。
- 分别断开测距机、IMU、GNSS，确认设备按预期关机且 RTT 能指出失败模块。
- 验证未按电源键的外部上电/复位不会进入应用，按下电源键能锁存 PA10 并正常启动。
- 验证开机按压必须先松开才允许后续按键操作，以及再次按住满 3 s 时无需松手立即关机。
- 验证 2600 mV 欠压关机。
- 用标准电压源核对 ADC 电压和五档电池图标阈值。

### 19.2 按键与模式

- 验证单击切换顺序及 600 ms 延迟。
- 验证三、四、五、六、七、八击，确认其他点击次数无动作。
- 在单次、连续和多功能测量中切换模式，确认测距机停止、旧数据清空、IMU/GNSS 电源符合新模式。
- 测试运行中按模式键，确认模式不变化；短按电源能停止测试。

### 19.3 测距

- 分别构造单目标、仅首目标、仅末目标和多个目标，核对 F/E、距离和中间目标过滤。
- 用串口抓包测量同一轮各返回帧的最大间隔，确认 40 ms/200 ms 阈值有余量。
- 连续模式确认 1 Hz、正好发布 10 轮后停止，且最后结果保留。
- 连续过程中提前短按停止；无目标、部分轮数和超时场景核对计数。
- 验证大于 9999 m 时 LCD 的实际表现，并决定是否需要溢出提示。

### 19.4 多功能和坐标

- 室外获得 GNSS 定位，核对 N/S/E/W、纬经度每秒切换和 `DDD° MM.mmmm′` 格式。
- 使用默认 `PIt=0.0°`、`HIt=+90.0°`、`HEr=0.0°`，确认激光朝上显示正俯仰、朝下显示负俯仰，并验证北、东、南、西分别显示约 `0°/90°/180°/270°`，再用已知方位、距离和高差的控制点记录剩余偏差并确定正式补偿值。
- 四击进入设置，逐页验证 `PIt`、`HIt`、`HEr` 的范围、`0.1°` 步进、按住连调和双键切页；核对距离区实时角度、高程区补偿值、补偿正负符号以及所有设置页均不点亮小数点。
- 在参数设置页按住电源键超过 3 s，确认补偿值继续增加且设备不会关机；松开后不能误触发保存。退出设置后再确认长按电源键 3 s 可以正常关机。
- 分别验证 `SAVE` 保存和模式键取消；断电重启后只有已保存值继续有效。
- 分别验证首、末目标坐标和每 2 s 切换。
- 在 GNSS 未定位、IMU 无帧、定位恢复和数据中断场景核对横杠及重新测量要求。
- 评估运动状态下约 1 s 非同步取数带来的误差。

### 19.5 测试模式和存储

- 记录至少 2 分钟测量时刻，确认立即首测和后续 12 s 间隔。
- 单笔超时、提前停止、长按关机后重新上电，核对计数和模式初始状态。
- 长时间连续耐久测试期间确认每笔测量不再擦 Flash，正常关机时只保存一次。
- 分别验证长按关机后计数保留、直接拔电后本次增量丢失、三击清零后正常关机才持久化。
- 检查每个发布固件的 map 文件，确保任何可加载段均未进入 `0x0801F800～0x0801FFFF`。

## 20. 代码文件索引

| 文件 | 主要职责 |
| --- | --- |
| `src/main.c` | 程序入口、调试初始化、RTOS 启动和致命错误处理 |
| `src/app/app_tasks.c` | 启动、I/O、控制、显示、四种模式、测距聚合和坐标计算 |
| `src/app/app_state.c` | 跨任务状态快照和临界区保护 |
| `src/app/module_selftest.c` | 三模块上电自检和测距协议预热 |
| `src/app/measure_counter.c` | 测量计数、三个角度补偿值及片内 Flash 保存 |
| `src/app/power_manager.c` | 模块总关断和运行/故障电源状态 |
| `src/app/app_log.c` | 带时间、任务和模块标签的 RTT 日志 |
| `src/app/app_debug.c` | RTT、CmBacktrace 和断言支持 |
| `src/app/app_runtime_stats.c` | FreeRTOS DWT 运行时间计数器接口，不再周期打印任务统计 |
| `src/modules/rangefinder.c` | DYC-15A 命令封装、帧解析和首末目标识别 |
| `src/modules/jy901b.c` | JY901B 配置、校准、角度解析和航向补偿 |
| `src/modules/bv220.c` | `$GNGGA` 校验、解析和坐标转换 |
| `src/drivers/keys.c` | 两按键消抖、短按和长按事件 |
| `src/drivers/battery.c` | 电压换算和电量分档 |
| `src/drivers/lcd_segments.c` | 27 位数字和图标到 LCD RAM 的映射 |
| `src/drivers/cs1622.c` | CS1622 命令及 RAM 写入时序 |
| `src/bsp/bsp_uart.c` | 四路 UART、DMA 接收和软件环形缓冲 |
| `src/bsp/bsp_adc.c` | ADC 校准、DMA 采样和去极值平均 |
| `inc/bsp/board_config.h` | 引脚、波特率、阈值、超时和模式开关 |
| `inc/app/measure_types.h` | 电池、测距、姿态和 GNSS 数据类型 |
| `inc/FreeRTOSConfig.h` | FreeRTOS 调度、堆、断言和统计配置 |
| `linker/n32l40x_flash.ld.in` | 根据构建参数生成 Flash/RAM 链接布局 |
| `CMakeLists.txt` | 源文件、工具链、编译链接选项、126 KB 应用区限制和固件产物 |
